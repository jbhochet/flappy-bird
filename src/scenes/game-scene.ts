class GameScene extends Phaser.Scene {
    constructor() {
        super({
            key: 'GameScene',
        })
    }

    preload() {}

    create() {}

    update(time: number, delta: number): void {}
}

export default GameScene
